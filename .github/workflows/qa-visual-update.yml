name: QA visual update

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  update_snapshots:
    if: false
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - run: echo "Maintainer-only — run visual update with secrets; do not run on untrusted forks."
