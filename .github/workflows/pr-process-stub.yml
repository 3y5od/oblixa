name: PR process stub

# Phase 14 — placeholder for Danger, label bots, or required reviewers in org settings.
# This job stays green so the workflow does not appear cancelled when branch protection lists it.

on:
  pull_request:
    branches: [main, master]

permissions:
  contents: read
  pull-requests: read

jobs:
  noop_documentation:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Stub (see workflow header)
        run: |
          echo "PR process stub: optional migrations/generated/label gates live in org automation or branch protection."
          echo "See CODEOWNERS and ci.yml for enforced checks."
