# V9 Supersession Bridge

V9 remains covered by the repository's `*.v9.test.ts` regression gates and `src/lib/v9-release-contract.ts` compatibility catalog.

`docs/v10.md` is the active release contract for current implementation work. This file is intentionally non-authoritative for new completion proof; it exists so older references to `docs/v9.md` resolve to a privacy-safe bridge instead of a missing artifact.

Supersession evidence:

- `docs/v9.md` is superseded by `docs/v10.md`.
- Legacy opaque log archives are superseded by structured V10 release evidence records.
- V9 regression gates must continue passing until explicit removal evidence is recorded.
