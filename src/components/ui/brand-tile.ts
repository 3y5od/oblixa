/**
 * Shared brand "O" tile for light surfaces — a neutral gray seal used
 * consistently across auth, marketing, and external pages (accent stays reserved
 * for actions; the dashboard sidebar carries its own dark-context variant, and
 * the account avatar keeps its own seal). Sizing and glyph font are applied per
 * surface alongside this base.
 */
export const BRAND_TILE_CLASS =
  "inline-flex items-center justify-center rounded-[4px] border border-[color:color-mix(in_oklab,var(--border-subtle)_88%,transparent)] bg-[var(--surface-contrast)] text-[var(--text-primary)]";
