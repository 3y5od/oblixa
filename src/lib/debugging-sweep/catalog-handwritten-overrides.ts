import type { SweepItem } from "./catalog-types";

/** Optional last-merge overrides; keep empty unless a hotfix row must ship outside the generator. */
export const HANDWRITTEN_SWEEP_OVERRIDES: readonly SweepItem[] = [];
