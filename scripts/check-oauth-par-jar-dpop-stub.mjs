#!/usr/bin/env node
/** PAR/JAR/DPoP/mTLS mesh not in use — stub satisfies plan todo until service mesh ships. */
console.log(JSON.stringify({ ok: true, checkId: "oauth-par-jar-dpop-mtls-spiffe", mode: "stub" }, null, 2));
process.exit(0);
